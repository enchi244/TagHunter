# Local tool settings (Claude preview config etc.)
.claude/

# The paid product and lead magnet must never be committed:
# anything in the repo can end up public. Deliver them through Lemon Squeezy / email only.
*.pdf

# Secrets and credentials
.env
.env.*
.dev.vars
*.pem
*.key
*.p12
secrets/

# Dependencies and build output (none used today, ignored in case they appear)
node_modules/
dist/
.wrangler/

# Editor and OS junk
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
*.log
*.bak
*.tmp
